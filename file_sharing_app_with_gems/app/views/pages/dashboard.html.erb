<h1>Welcome, <%= current_user.username %>!</h1>

<h2>Your Recent Files</h2>
<% if @recent_files.any? %>
  <ul class="file-list">
    <% @recent_files.each do |file| %>
      <li class="file-item">
        <span class="file-name"><%= file.name %>
          <span class="file-date">(Uploaded on: <%= file.created_at.strftime("%d %B %Y") %>)</span>
        </span>
        <%= link_to "Download", download_file_path(file), class: 'download-link' %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="no-files-message">You haven't uploaded any files yet.</p>
<% end %>

<div class="manage-files-container">
  <%= link_to "Manage All Files", files_path, class: 'manage-files-link' %>
</div>
