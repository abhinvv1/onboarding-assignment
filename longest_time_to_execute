awk '/Started/ {for(i=1;i<=NF;i++) if($i~/^(GET|POST|PATCH|DELETE|PUT)$/){r=$i FS $(i+1);next}} /Completed/ {for(i=1;i<=NF;i++) if($i=="in"&&$(i+1)~/[0-9]+ms/){print $(i+1),r;next}}' "$1" | sort -rn | head -1
